diff --git a/agent-suites/fixtures/sample-app/src/shell-sink.ts b/agent-suites/fixtures/sample-app/src/shell-sink.ts
new file mode 100644
--- /dev/null
+++ b/agent-suites/fixtures/sample-app/src/shell-sink.ts
@@ -0,0 +1,6 @@
+/** Toy free-text→shell argv sink for anti-thrash suite seeds (not production code). */
+export function buildShellArgv(userText: string): string[] {
+  // Intentionally joins free text into a shell-shaped argv for review thrash fixtures.
+  // Theme: shell-argv-free-text-sinks — tip commit carries Theme: for cold archaeology.
+  return ['sh', '-c', `echo ${userText}`]
+}
