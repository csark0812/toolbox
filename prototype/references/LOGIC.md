# LOGIC branch

**Question:** "Does this logic / state model feel right?"

Build a tiny interactive terminal (or REPL-style) app that pushes the state machine through cases hard to reason about on paper.

- Keep I/O minimal — stdin/stdout or a single script entrypoint.
- After every transition, print the full relevant state.
- Use realistic edge cases from the design question, not happy-path only.
- Stack-agnostic — `node`, `python`, `bun`, etc. per project norms.
