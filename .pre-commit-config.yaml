repos:
  - repo: local
    hooks:
      - id: skeleton-validate-staged
        name: skeleton validate changed (staged)
        entry: npm run validate:changed -- --staged
        language: system
        pass_filenames: false
